Helm Chart for kafka_exporter
==============

Example Usage 
 - helm install --name=kafka-exporter kafka-exporter

You should set internal kafka service url with kafka.endpoint variable, before install chart.

